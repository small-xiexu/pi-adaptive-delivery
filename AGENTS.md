# Pi Adaptive Delivery 协作规则

## 适用范围

- 本文件适用于仓库根目录及全部子目录。
- 所有沟通、方案、计划和文档使用简体中文；代码标识符、命令和协议字段保持英文。

## 事实源与进度

- `docs/技术方案.md` 是当前设计事实源。
- `docs/实施计划.md` 是唯一开发进度台账。
- 每完成一个可独立验证的事项，立即更新实施计划中的状态和证据。
- 未验证事项不得标记完成；阻塞事项必须记录断点、原因和下一步。

## 工程边界

- 同一个 cwd/worktree 同时只允许一个 writer。
- 保持 Package 最小，不实现第二套计划系统、子 Agent runtime、CI 或发布系统。
- 只使用 Pi 和 `pi-subagents` 的公开 API，不导入其内部路径。
- 权限、路径、批准来源、lease 和恢复判断失败时默认关闭权限。
- 测试默认禁网，使用临时目录和 fake provider，不读取用户凭证或调用真实模型。
- 不修改与当前任务无关的用户文件和本地 IDE 元数据。

## 验证与发布

- 先运行覆盖当前改动的最小验证，再按影响范围扩大。
- 关键边界必须同时具有单元测试和真实 Pi/临时 Git 集成证据。
- 未经用户单独授权，不 commit、push、创建 PR、npm publish 或部署。
